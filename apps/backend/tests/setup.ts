// Cloudflare Workers pool setup. Executed once per test worker.
// Keep this minimal: the Workers runtime does not support jest-dom or other
// DOM-tied matchers. Shared helpers live in backend/tests/helpers.

export {}
