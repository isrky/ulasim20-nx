{
  "name": "ui-layout",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/layout/src",
  "projectType": "library",
  "tags": ["type:ui", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/ui/layout" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/ui/layout" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/ui/layout",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/ui/layout/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      } }
  }
}
