{
  "name": "ui-map",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/map/src",
  "projectType": "library",
  "tags": ["type:ui", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/ui/map" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/ui/map" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run --project=frontend libs/ui/map" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/ui/map",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/ui/map/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      } }
  }
}
