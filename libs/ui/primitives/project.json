{
  "name": "ui-primitives",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/ui/primitives/src",
  "projectType": "library",
  "tags": ["type:ui", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/ui/primitives" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/ui/primitives" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/ui/primitives",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/ui/primitives/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      } }
  }
}
