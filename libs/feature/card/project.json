{
  "name": "feature-card",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/feature/card/src",
  "projectType": "library",
  "tags": ["type:feature", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/feature/card" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/feature/card" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/feature/card",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored libs/feature/card"
        ],
        "parallel": false
      } }
  }
}
