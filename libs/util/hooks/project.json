{
  "name": "util-hooks",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/util/hooks/src",
  "projectType": "library",
  "tags": ["type:util", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/util/hooks" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p libs/util/hooks" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/util/hooks",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored libs/util/hooks"
        ],
        "parallel": false
      } }
  }
}
