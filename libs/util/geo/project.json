{
  "name": "util-geo",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/util/geo/src",
  "projectType": "library",
  "tags": ["type:util", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --build libs/util/geo" }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --build libs/util/geo" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --project=node libs/util/geo" }
    },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/util/geo",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/util/geo/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      } }
  }
}
