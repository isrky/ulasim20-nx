{
  "name": "util-validation",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/util/validation/src",
  "projectType": "library",
  "tags": ["type:util"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/util/validation" }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/util/validation" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "biome lint libs/util/validation" }
    }
  }
}
