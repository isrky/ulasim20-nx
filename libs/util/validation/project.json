{
  "name": "util-validation",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/util/validation/src",
  "projectType": "library",
  "tags": ["type:util", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/util/validation" }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/util/validation" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "biome lint libs/util/validation",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/util/validation/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      }
    }
  }
}
