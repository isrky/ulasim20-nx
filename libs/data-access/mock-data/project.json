{
  "name": "data-access-mock-data",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/data-access/mock-data/src",
  "projectType": "library",
  "tags": ["type:data-access", "scope:shared"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/data-access/mock-data" } },
    "type-check": { "executor": "nx:run-commands", "options": { "command": "tsc --build libs/data-access/mock-data" } },
    "lint": { "executor": "nx:run-commands", "options": {
        "commands": [
          "biome lint libs/data-access/mock-data",
          "eslint --config libs/config/eslint/eslint.config.mjs --no-warn-ignored 'libs/data-access/mock-data/**/*.{ts,tsx,js,jsx}'"
        ],
        "parallel": false
      } }
  }
}
